# 013 フロントエンド - P-05 Q&A一覧画面

## 概要
全Q&Aを一覧表示し、キーワード検索・ステータス絞り込み・ソートができる画面を実装する。

## 画面パス
`/qa` → `QaListPage.tsx`

## Todo

### UI実装（`src/pages/QaListPage.tsx`）
- [ ] キーワード検索欄（テキスト入力）を配置する
- [ ] 「検索」ボタンを配置する
- [ ] ステータス絞り込み（全件 / 未回答のみ / 回答済のみ）をプルダウンまたはラジオボタンで配置する
- [ ] ソート選択（新着順 / 古い順）をプルダウンまたはラジオボタンで配置する
- [ ] 一覧テーブルを実装する（カラム：タイトル・質問者・ステータス・質問日時）
  - [ ] タイトルはリンク（クリックで `/questions/:id` へ遷移）
  - [ ] 質問日時は `YYYY/MM/DD HH:mm` 形式で表示する
  - [ ] ステータスは「未回答」/「回答済」で表示する
- [ ] 「戻る」ボタンを配置する → クリックで `/` へ遷移する

### API連携
- [ ] 画面初期表示時に `GET /api/questions?sort=desc` を呼ぶ（全件）
- [ ] 「検索」ボタン押下時に `GET /api/questions?keyword=...&status=...&sort=...` を呼ぶ
- [ ] 検索・絞り込みはAPIに委ねる（フロントエンドで再フィルタしない）
- [ ] APIエラー時に適切なエラーメッセージを表示する

### 動作確認
- [ ] 全質問（未回答・回答済）が一覧表示されることを確認する
- [ ] キーワード検索でタイトル・質問内容・回答内容が絞り込まれることを確認する
- [ ] ステータス絞り込みが正しく動作することを確認する
- [ ] ソート切り替えが正しく動作することを確認する
- [ ] タイトルクリックで質問詳細画面に遷移することを確認する
