# 006 バックエンド - 質問削除トランザクション

## 概要
質問削除時に、紐づく全回答を同一トランザクション内で論理削除する処理を実装する。

## Todo

### サービス（`QuestionService.java`）
- [ ] `deleteQuestion(Long id)` に `@Transactional` を付与する
- [ ] 対象質問が存在しない（または論理削除済み）場合は `ResourceNotFoundException`（404）
- [ ] 質問の `deleted_at` に現在時刻をセットして保存する
- [ ] 紐づく有効回答（`deleted_at IS NULL`）を一括取得し、全件の `deleted_at` に現在時刻をセットして保存する
- [ ] 個別の回答削除API（`AnswerService.deleteAnswer`）は呼ばずにサービス層で直接処理する

### トランザクション境界の確認
- [ ] 質問・回答の両方が正常に論理削除されることを確認する
- [ ] 回答の削除処理中に例外が発生した場合、質問の削除もロールバックされることを確認する

### 動作確認
- [ ] 回答が複数ある質問を削除し、`GET /api/questions/{id}` が 404 を返すことを確認する
- [ ] 削除後、回答も取得されないことを確認する（`GET /api/questions/{id}` の `answers` に含まれない）
