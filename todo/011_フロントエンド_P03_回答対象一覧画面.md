# 011 フロントエンド - P-03 回答対象一覧画面

## 概要
未回答質問の一覧を表示し、キーワード検索・ソートができる画面を実装する。

## 画面パス
`/questions/unanswered` → `UnansweredListPage.tsx`

## Todo

### UI実装（`src/pages/UnansweredListPage.tsx`）
- [ ] キーワード検索欄（テキスト入力）を配置する
- [ ] 「検索」ボタンを配置する
- [ ] ソート選択（新着順 / 古い順）をプルダウンまたはラジオボタンで配置する
- [ ] 一覧テーブルを実装する（カラム：タイトル・質問者・質問日時・ステータス）
  - [ ] タイトルはリンク（クリックで `/questions/:id` へ遷移）
  - [ ] 質問日時は `YYYY/MM/DD HH:mm` 形式で表示する
  - [ ] ステータス列は「未回答」固定表示
- [ ] 「戻る」ボタンを配置する → クリックで `/` へ遷移する

### API連携
- [ ] 画面初期表示時に `GET /api/questions?status=UNANSWERED&sort=desc` を呼ぶ
- [ ] 「検索」ボタン押下時に `GET /api/questions?status=UNANSWERED&keyword=...&sort=...` を呼ぶ
- [ ] 検索・絞り込みはAPIに委ねる（フロントエンドで再フィルタしない）
- [ ] API エラー時に適切なエラーメッセージを表示する

### 動作確認
- [ ] 未回答質問のみ一覧表示されることを確認する
- [ ] キーワード検索でタイトル・質問内容が絞り込まれることを確認する
- [ ] ソート切り替えが正しく動作することを確認する
- [ ] タイトルクリックで質問詳細画面に遷移することを確認する
