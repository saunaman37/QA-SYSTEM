# 008 フロントエンド - 共通コンポーネント

## 概要
複数画面で使用する共通UIコンポーネント（削除確認ダイアログ、回答修正モーダル）を実装する。

## Todo

### 削除確認ダイアログ（`src/components/DeleteConfirmDialog.tsx`）
- [ ] ブラウザ標準の `confirm` は使用せず、独自モーダルとして実装する
- [ ] props：`isOpen`（boolean）・`message`（string）・`onConfirm`（() => void）・`onCancel`（() => void）
- [ ] 「削除する」ボタン押下時：`onConfirm` を呼ぶ
- [ ] 「キャンセル」ボタン押下時：`onCancel` を呼ぶ
- [ ] 質問削除時のメッセージ：「この質問を削除します。この操作は取り消せません。（管理者復元機能は v1.0.0 では未対応）」
- [ ] 回答削除時のメッセージ：「この回答を削除します。この操作は取り消せません。（管理者復元機能は v1.0.0 では未対応）」

### 回答登録モーダル（`src/components/AnswerFormModal.tsx`）
- [ ] props：`isOpen`（boolean）・`questionId`（number）・`onSuccess`（() => void）・`onCancel`（() => void）
- [ ] フィールド：回答内容（テキストエリア、500文字以内）・回答者（テキスト、50文字以内）
- [ ] 「登録」ボタン押下時：フロントバリデーション → `POST /api/questions/{id}/answers` → 成功時 `onSuccess` を呼ぶ
- [ ] 「キャンセル」ボタン押下時：`onCancel` を呼ぶ
- [ ] バリデーションエラーを各フィールド直下に表示する
- [ ] APIエラー（`fieldErrors`）を各フィールド直下に表示する

### 回答修正モーダル（`src/components/AnswerEditModal.tsx`）
- [ ] props：`isOpen`（boolean）・`answer`（Answer | null）・`onSuccess`（() => void）・`onCancel`（() => void）
- [ ] 初期値に対象回答の既存データ（回答内容・回答者）を展開して表示する
- [ ] 「更新」ボタン押下時：バリデーション → `PUT /api/answers/{answerId}` → 成功時 `onSuccess` を呼ぶ
- [ ] 「キャンセル」ボタン押下時：`onCancel` を呼ぶ

### エラー表示コンポーネント（`src/components/ErrorMessage.tsx`）
- [ ] `message`（string）を受け取り、フィールド直下のインラインエラーとして表示する

### 動作確認
- [ ] 削除確認ダイアログが開閉できることを確認する
- [ ] 回答登録モーダルでバリデーションエラーが表示されることを確認する
- [ ] 回答修正モーダルに既存データが展開されることを確認する
